mi_events_sns_topic_arn: ${MI_EVENTS_SNS_TOPIC_ARN:test_topic}
mi_events_sqs_queue_for_event_enrichment_url: ${MI_EVENTS_SQS_QUEUE_FOR_EVENT_ENRICHMENT_URL:test_url}

springdoc:
  show-actuator: true

  swagger-ui:
    path: /swagger

management:
  endpoints:
    web:
      base-path:
      path-mapping:
        health: /_status

cloud:
  aws:
    stack:
      auto: false

jolokia:
  username: ${MI_BASIC_AUTH_USERNAME:test_username}
  password: ${MI_BASIC_AUTH_PASSWORD:test_password}
